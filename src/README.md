# Contract Registry â€” src/

This folder contains the implementation surface for Contract Registry.

Minimum intent:
- load trust_bundle.json (NeverLost layer)
- validate/ingest contract manifests
- emit registry index (deterministic)
- produce receipts compatible with NeverLost v1
