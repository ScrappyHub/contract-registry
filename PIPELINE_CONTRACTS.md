# Pipeline Contracts â€” Contract Registry (v1)

Inputs: packets/inbox + trust bundle + allowed_signers
Rules: verify signature, enforce namespace, hash artifacts, receipt everything
Outputs: packets/receipts + (future) registry index
