# Contract Registry â€” SPEC (v1)

Contract Registry is the canonical catalog of contracts (schemas + invariants + pipeline contracts).
It emits deterministic indices and receipts; it does not replace Covenant Gate or TRIAD/Watchtower.
